#Tuples and List difference is that the Tuples can not be modified and list can be modified as the tuples can not be add , replace the items in the tuples
names = ("Smit" , "Shivanshi" , "Shweta")

print(len(names))
print(names.index("Smit"))

print(names[-1])
print(names[0])

print("Smit" in names)

print(sorted(names))
